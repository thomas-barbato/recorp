# AGENTS.md

## UI Text and Translation
- Any new user-facing text added to the UI must use English as the source string.
- The same change must add the corresponding French translation in the locale catalogs.
- This applies to templates, JavaScript UI text, modals, labels, buttons, notifications, and status messages.
